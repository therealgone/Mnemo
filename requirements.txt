# Requires Python 3.12 (chromadb/sentence-transformers/torch don't yet have
# working Python 3.14 wheels on Windows as of this writing — see README).
#
# Install with the CPU-only torch index, since everything here runs on CPU +
# local Ollama and the plain PyPI torch wheel pulls in unused CUDA/nvidia-*
# packages (~2GB of dead weight) on Linux:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
pywebview==5.4

langchain==1.3.17
langchain-core==1.6.0
langchain-classic==1.0.8
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-chroma==1.1.0
langchain-huggingface==1.2.2
langchain-ollama==1.1.0
rank-bm25
chromadb==1.5.9
sentence-transformers==6.0.0
